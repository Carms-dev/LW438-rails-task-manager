<%= simple_form_for @task, html: { class: 'form' } do |f| %>
    <%= f.input :title %>
    <%= f.input :details %>

    <% if !@task.new_record? %>
        <%= f.input :completed %>
    <% end %>
    <%= f.submit %>
<% end %>